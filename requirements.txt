# ── Runtime (install these on the Pi) ──
#   pip install -r requirements.txt
# System package also required (not a pip dep):
#   sudo apt install ghostscript    (renders PDFs to PCL for the printer)

python-telegram-bot[job-queue]>=21
requests
psutil
python-dotenv
Pillow
